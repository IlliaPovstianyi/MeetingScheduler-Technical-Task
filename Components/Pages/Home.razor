@page "/"
@inject NavigationManager NavigationManager

@code {
    bool IsAuthenticated = false; //Temporary
    protected override void OnInitialized()
    {
        if (!IsAuthenticated)
        {
            NavigationManager.NavigateTo("/authentication");
        }
    }
}